name: CI
on:
  schedule:
  - cron: '0 1 * * *'
  workflow_dispatch:
permissions: write-all

jobs:
  trigger-build:
    uses: sabbirba/rebuild/.github/workflows/build.yml@main
    with:
      org: ReBuild
    secrets: inherit
